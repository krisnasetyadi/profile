/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'cdn.dribbble.com',
            'firebasestorage.googleapis.com'    
        ]
      },
}

module.exports = nextConfig
