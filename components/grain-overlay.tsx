/**
 * GrainOverlay — fixed full-page SVG feTurbulence noise layer.
 * Makes flat dark colors feel physical and premium (Godly / Linear secret weapon).
 * Pure CSS, zero runtime JS overhead.
 */
export function GrainOverlay() {
  return <div aria-hidden="true" className="grain-overlay" />;
}
